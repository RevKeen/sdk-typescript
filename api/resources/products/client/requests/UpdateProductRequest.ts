// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         id: "prod_123"
 *     }
 */
export interface UpdateProductRequest {
    id: string;
    name?: string;
    description?: string | null;
    amount_minor?: number;
    currency?: string;
    interval?: string | null;
    interval_count?: number | null;
    trial_days?: number;
    is_active?: boolean;
    is_archived?: boolean;
    /** Fulfillment type for the product. */
    fulfillment_type?: UpdateProductRequest.FulfillmentType;
    /** How billing dates are calculated. same_day: Bill on same day as start. day_of_month: Bill on specific day (1-31). last_day: Bill on last day of month. */
    billing_anchor_rule?: UpdateProductRequest.BillingAnchorRule;
    billing_anchor_day?: number | null;
    /** When first payment is collected. immediate: Charge on start. next_anchor: Charge on first scheduled date. prorate: Prorate until first date. */
    first_charge_behavior?: UpdateProductRequest.FirstChargeBehavior;
    /** How subscription ends. until_canceled: Runs forever. fixed_payments: Ends after N billing cycles. */
    end_behavior?: UpdateProductRequest.EndBehavior;
    max_payments?: number | null;
    /** Arbitrary key-value metadata for the product */
    metadata?: Record<string, unknown>;
}

export namespace UpdateProductRequest {
    /** Fulfillment type for the product. */
    export const FulfillmentType = {
        None: "none",
        Digital: "digital",
        Physical: "physical",
    } as const;
    export type FulfillmentType = (typeof FulfillmentType)[keyof typeof FulfillmentType];
    /** How billing dates are calculated. same_day: Bill on same day as start. day_of_month: Bill on specific day (1-31). last_day: Bill on last day of month. */
    export const BillingAnchorRule = {
        SameDay: "same_day",
        DayOfMonth: "day_of_month",
        LastDay: "last_day",
    } as const;
    export type BillingAnchorRule = (typeof BillingAnchorRule)[keyof typeof BillingAnchorRule];
    /** When first payment is collected. immediate: Charge on start. next_anchor: Charge on first scheduled date. prorate: Prorate until first date. */
    export const FirstChargeBehavior = {
        Immediate: "immediate",
        NextAnchor: "next_anchor",
        Prorate: "prorate",
    } as const;
    export type FirstChargeBehavior = (typeof FirstChargeBehavior)[keyof typeof FirstChargeBehavior];
    /** How subscription ends. until_canceled: Runs forever. fixed_payments: Ends after N billing cycles. */
    export const EndBehavior = {
        UntilCanceled: "until_canceled",
        FixedPayments: "fixed_payments",
    } as const;
    export type EndBehavior = (typeof EndBehavior)[keyof typeof EndBehavior];
}
